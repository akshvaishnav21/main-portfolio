# Optional: verified production origin is the default.
SITE_URL=https://main-portfolio-eight-tau.vercel.app
# Optional, server-only. No token is needed to run the portfolio.
GITHUB_TOKEN=
# Deterministic offline builds/tests can omit optional star counts.
GITHUB_STARS_DISABLED=0
